import searchYouTube2 from '../lib/searchYouTube2.js';

var Statistics = (props) => {
  var stats = 
  searchYouTube2(a, ()=> (stats = data));

  return (

    <div>  </div>
  );

};

export default Statistics;